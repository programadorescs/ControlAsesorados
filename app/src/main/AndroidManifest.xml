<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ControlAsesoradosApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlAsesorados"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.ejercicio.OperacionEjercicioActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.rutina.OperacionRutinaActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.crearcuenta.CrearCuentaActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.maquina.OperacionMaquinaActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.ControlAsesorados.NoActionBar" />
        <activity
            android:name=".presentation.ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>